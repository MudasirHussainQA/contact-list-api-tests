name: 🌙 Nightly Full Test Suite

on:
  schedule:
    # Run every night at 2 AM UTC
    - cron: '0 2 * * *'
  workflow_dispatch:
    inputs:
      test_type:
        description: 'Test type to run'
        required: true
        default: 'all'
        type: choice
        options:
          - all
          - api
          - ui
          - cross-browser

permissions:
  contents: read
  pages: write
  id-token: write

env:
  NODE_VERSION: '20.x'

jobs:
  # Job 1: API Tests (Fast feedback)
  api-tests:
    name: 🔧 API Tests
    runs-on: ubuntu-latest
    timeout-minutes: 10
    
    steps:
      - name: 📥 Checkout repository
        uses: actions/checkout@v4

      - name: 🟢 Setup Node.js ${{ env.NODE_VERSION }}
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'

      - name: 📦 Install dependencies
        run: npm ci

      - name: 🧪 Run API tests
        run: npm run test:api
        env:
          CI: true

      - name: 📊 Upload API test results
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: api-test-results
          path: playwright-report/
          retention-days: 7

  # Job 2: Cross-browser testing (nightly only)
  cross-browser-tests:
    name: 🌐 Cross-Browser Tests (${{ matrix.browser }})
    runs-on: ubuntu-latest
    timeout-minutes: 45
    strategy:
      fail-fast: false
      matrix:
        browser: [chromium, firefox, webkit]
    
    steps:
      - name: 📥 Checkout repository
        uses: actions/checkout@v4

      - name: 🟢 Setup Node.js ${{ env.NODE_VERSION }}
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'

      - name: 📦 Install dependencies
        run: npm ci

      - name: 🎭 Install Playwright browsers
        run: npx playwright install ${{ matrix.browser }} --with-deps

      - name: 🧪 Run UI tests on ${{ matrix.browser }}
        run: npx playwright test --project=ui-tests-${{ matrix.browser }} --reporter=blob
        env:
          CI: true

      - name: 📊 Upload test results
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: blob-report-${{ matrix.browser }}
          path: blob-report/
          retention-days: 7

  # Job 3a: Generate API Test Report
  generate-api-report:
    name: 📊 Generate API Report
    runs-on: ubuntu-latest
    needs: [api-tests, cross-browser-tests]
    if: always()
    
    steps:
      - name: 📥 Checkout repository
        uses: actions/checkout@v4

      - name: 📥 Download API test results
        uses: actions/download-artifact@v4
        with:
          name: api-test-results
          path: ./all-test-results/api-test-results
        continue-on-error: true

      - name: 🟢 Setup Node.js ${{ env.NODE_VERSION }}
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}

      - name: 📦 Install dependencies
        run: npm ci

      - name: 📊 Organize API report
        run: |
          echo "📁 Organizing API report..."
          mkdir -p ./pages-deploy/nightly-reports/${{ github.run_number }}/api
          
          if [ -d "./all-test-results/api-test-results" ]; then
            echo "✅ Found API test results"
            cp -r ./all-test-results/api-test-results/* ./pages-deploy/nightly-reports/${{ github.run_number }}/api/
            echo "✅ API report organized successfully"
            ls -la ./pages-deploy/nightly-reports/${{ github.run_number }}/api/ | head -10
          else
            echo "❌ No API test results found"
          fi

      - name: 📤 Upload API Report (artifact)
        uses: actions/upload-artifact@v4
        with:
          name: pages-deploy-nightly
          path: ./pages-deploy
          retention-days: 30

  # Job 3b: Generate Browser Test Report
  generate-browser-report:
    name: 🌐 Generate Browser Report
    runs-on: ubuntu-latest
    needs: [cross-browser-tests, generate-api-report]
    if: always()
    
    steps:
      - name: 📥 Checkout repository
        uses: actions/checkout@v4

      - name: 📥 Download pages-deploy artifact
        uses: actions/download-artifact@v4
        with:
          name: pages-deploy-nightly
          path: ./pages-deploy
        continue-on-error: true

      - name: 📥 Download Chromium test results
        uses: actions/download-artifact@v4
        with:
          name: blob-report-chromium
          path: ./all-blob-reports
        continue-on-error: true

      - name: 📥 Download Firefox test results
        uses: actions/download-artifact@v4
        with:
          name: blob-report-firefox
          path: ./all-blob-reports
        continue-on-error: true

      - name: 📥 Download WebKit test results
        uses: actions/download-artifact@v4
        with:
          name: blob-report-webkit
          path: ./all-blob-reports
        continue-on-error: true

      - name: 🟢 Setup Node.js ${{ env.NODE_VERSION }}
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}

      - name: 📦 Install dependencies
        run: npm ci

      - name: 📊 Organize browser report
        run: |
          echo "📁 Organizing browser report..."
          mkdir -p ./pages-deploy/nightly-reports/${{ github.run_number }}/browser
          
          echo "🔍 Checking downloaded artifacts..."
          if [ -d "./all-blob-reports" ]; then
            echo "Contents of all-blob-reports:"
            ls -laR ./all-blob-reports
            
            # Count actual report files
            REPORT_COUNT=$(find ./all-blob-reports -name "*.zip" -o -name "*.jsonl" 2>/dev/null | wc -l)
            echo "Found $REPORT_COUNT report files"
            
            if [ $REPORT_COUNT -gt 0 ]; then
              echo "✅ Found blob reports - merging all browsers..."
              
              echo "🔄 Running merge-reports..."
              npx playwright merge-reports --reporter html ./all-blob-reports
              
              # Check if merge was successful
              if [ -d "./playwright-report" ]; then
                echo "✅ Merge successful, copying merged report..."
                cp -r ./playwright-report/* ./pages-deploy/nightly-reports/${{ github.run_number }}/browser/
                rm -rf ./playwright-report
                echo "✅ Browser report organized successfully"
                ls -la ./pages-deploy/nightly-reports/${{ github.run_number }}/browser/ | head -10
              else
                echo "⚠️ Merge failed - no playwright-report directory created"
              fi
            else
              echo "⚠️ No blob report files found in downloaded artifacts"
            fi
          else
            echo "⚠️ all-blob-reports directory does not exist"
          fi
      
      - name: 🌐 Setup GitHub Pages
        uses: actions/configure-pages@v4
        if: github.ref == 'refs/heads/main'
        continue-on-error: true

      - name: 📤 Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
        if: github.ref == 'refs/heads/main'
        with:
          path: ./pages-deploy
        continue-on-error: true

      - name: 🚀 Deploy to GitHub Pages
        uses: actions/deploy-pages@v4
        if: github.ref == 'refs/heads/main'
        continue-on-error: true

  # Job 4: Notifications
  notify:
    name: 📢 Notify Results
    runs-on: ubuntu-latest
    needs: [cross-browser-tests, generate-api-report, generate-browser-report]
    if: always()
    
    steps:
      - name: 📢 Send Slack notification with report links
        uses: 8398a7/action-slack@v3
        if: always()
        with:
          status: custom
          custom_payload: |
            {
              "text": "🎭 Nightly Test Reports",
              "attachments": [{
                "color": "#FF9800",
                "fields": [{
                  "title": "Repository",
                  "value": "${{ github.repository }}",
                  "short": true
                }, {
                  "title": "Run",
                  "value": "#${{ github.run_number }}",
                  "short": true
                }, {
                  "title": "Commit",
                  "value": "`${{ github.sha }}`",
                  "short": true
                }],
                "actions": [{
                  "type": "button",
                  "text": "🔧 API Test Report",
                  "url": "https://${{ github.repository_owner }}.github.io/${{ github.event.repository.name }}/nightly-reports/${{ github.run_number }}/api/"
                }, {
                  "type": "button",
                  "text": "🌐 Browser Test Report",
                  "url": "https://${{ github.repository_owner }}.github.io/${{ github.event.repository.name }}/nightly-reports/${{ github.run_number }}/browser/"
                }, {
                  "type": "button",
                  "text": "🔍 Workflow",
                  "url": "https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}"
                }]
              }]
            }
        env:
          SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}
